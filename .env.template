# Morph configuration
# Copy this file to .env and fill in the values

# OpenAI API key (required for AI-assisted mapping and evaluation)
OPENAI_API_KEY=your-openai-api-key-here

# GCP GSM credentials (required for accessing Airbyte source data and schema)
# This should contain the JSON content directly, not a path to a file
GCP_GSM_CREDENTIALS={"type":"service_account","project_id":"...","private_key_id":"..."}

# Add other environment variables as needed
