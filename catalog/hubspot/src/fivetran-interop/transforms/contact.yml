domain: hubspot.fivetran-interop
transforms:
  - name: contact
    from:
      - contacts: hubspot.contacts
    fields:
      _fivetran_deleted:
        expression: MISSING
        description: '{{ doc("_fivetran_deleted") }}'
      _fivetran_synced:
        expression: contacts._airbyte_extracted_at
        description: '{{ doc("_fivetran_synced") }}'
      id:
        expression: contacts.id
        description: The ID of the contact.
      property_email_1:
        expression: contacts.properties.email
        description: The email address of the contact.
      property_company:
        expression: contacts.properties.company
        description: The name of the contact's company.
      property_firstname:
        expression: contacts.properties.firstname
        description: The contact's first name.
      property_lastname:
        expression: contacts.properties.lastname
        description: The contact's last name.
      property_email:
        expression: contacts.properties.email
        description: The contact's email.
      property_createdate:
        expression: contacts.properties.createdate
        description: The date that the contact was created in your HubSpot account.
      property_jobtitle:
        expression: contacts.properties.jobtitle
        description: The contact's job title.
      property_annualrevenue:
        expression: contacts.properties.annualrevenue
        description: The contact's annual company revenue.
      property_hs_calculated_merged_vids:
        expression: contacts.properties.hs_calculated_merged_vids
        description:
          "List of mappings representing contact IDs that have been merged
          into the contact at hand. Format: <merged_contact_id>:<merged_at_in_epoch_time>;<second_merged_contact_id>:<merged_at_in_epoch_time>
          This field has replaced the `CONTACT_MERGE_AUDIT` table, which was deprecated
          by the Hubspot v3 CRM API.\n"
annotations:
  approved: true
  missing_fields:
    - _fivetran_deleted
  evaluation:
    source_stream_name: contacts
    target_table_name: contact
    name_match_score: 0.9
    score: 0.752
    explanation:
      Most fields have been successfully mapped with high confidence. '_fivetran_synced'
      was mapped to the correct '_airbyte_extracted_at' column. '_fivetran_deleted'
      field is missing, significantly affecting the overall score. The mapping expressions
      closely align with the expected fields, suggesting overall high mapping accuracy.
    field_mapping_evals:
      - name: _fivetran_deleted
        expression: MISSING
        score: 0.0
        explanation: Expression is 'MISSING', which indicates no mapping found.
      - name: _fivetran_synced
        expression: contacts._airbyte_extracted_at
        score: 1.0
        explanation: Mapped as recommended to '_airbyte_extracted_at' column.
      - name: id
        expression: contacts.id
        score: 1.0
        explanation: Mapped directly to 'contacts.id' which is a perfect match.
      - name: property_email_1
        expression: contacts.properties.email
        score: 0.7
        explanation:
          Mapped to 'contacts.properties.email', a likely match but not
          unique due to another email field.
      - name: property_company
        expression: contacts.properties.company
        score: 1.0
        explanation:
          Mapped directly to 'contacts.properties.company', indicating
          a perfect match.
      - name: property_firstname
        expression: contacts.properties.firstname
        score: 1.0
        explanation:
          Mapped directly to 'contacts.properties.firstname', indicating
          a perfect match.
      - name: property_lastname
        expression: contacts.properties.lastname
        score: 1.0
        explanation:
          Mapped directly to 'contacts.properties.lastname', indicating
          a perfect match.
      - name: property_email
        expression: contacts.properties.email
        score: 0.7
        explanation:
          Mapped to 'contacts.properties.email', a likely match but redundant
          due to another email field mapping.
      - name: property_createdate
        expression: contacts.properties.createdate
        score: 1.0
        explanation:
          Mapped directly to 'contacts.properties.createdate', indicating
          a perfect match.
      - name: property_jobtitle
        expression: contacts.properties.jobtitle
        score: 1.0
        explanation:
          Mapped directly to 'contacts.properties.jobtitle', indicating
          a perfect match.
      - name: property_annualrevenue
        expression: contacts.properties.annualrevenue
        score: 1.0
        explanation:
          Mapped directly to 'contacts.properties.annualrevenue', indicating
          a perfect match.
      - name: property_hs_calculated_merged_vids
        expression: contacts.properties.hs_calculated_merged_vids
        score: 0.9
        explanation:
          Mapped to 'contacts.properties.hs_calculated_merged_vids', close
          to perfect match but with potential for slight discrepancies.
