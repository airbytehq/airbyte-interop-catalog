domain: hubspot.fivetran-interop
transforms:
  - name: email_event_print
    from:
      - email_events: hubspot.email_events
    fields:
      _fivetran_synced:
        expression: email_events._airbyte_extracted_at
        description: '{{ doc("_fivetran_synced") }}'
      browser:
        expression: email_events.browser
        description: '{{ doc("email_event_browser") }}'
      id:
        expression: email_events.id
        description: The ID of the event.
      ip_address:
        expression: email_events.ipAddress
        description: '{{ doc("email_event_ip_address") }}'
      location:
        expression: email_events.location
        description: '{{ doc("email_event_location") }}'
      user_agent:
        expression: email_events.userAgent
        description: '{{ doc("email_event_user_agent") }}'
annotations:
  approved: true
  missing_fields: []
  evaluation:
    source_stream_name: email_events
    target_table_name: email_event_print
    table_match_score: 0.75
    completion_score: 0.8
    explanation: The table mapping suggests a strong correlation with email events,
      commonly found in both source and target schemas. Most fields have been appropriately
      matched to their equivalents.
    field_mapping_evals:
      - name: _fivetran_synced
        expression: email_events._airbyte_extracted_at
        score: 1.0
        explanation: Standard mapping from _fivetran_synced to _airbyte_extracted_at.
      - name: browser
        expression: email_events.browser
        score: 0.7
        explanation: "'browser' matched directly from 'email_events.browser', which
          is likely a direct mapping of user browsing data."
      - name: id
        expression: email_events.id
        score: 0.65
        explanation: "'id' matched to 'email_events.id', typically an identifier field
          directly shared between schemas."
      - name: ip_address
        expression: email_events.ipAddress
        score: 0.7
        explanation: Direct match of 'ip_address' to 'email_events.ipAddress', common
          mapping for IP recording.
      - name: location
        expression: email_events.location
        score: 0.7
        explanation: Matched 'location' to 'email_events.location', generally indicating
          the same concept.
      - name: user_agent
        expression: email_events.userAgent
        score: 0.65
        explanation: Directly mapped 'user_agent' to 'email_events.userAgent', reflecting
          similar user metadata tracking.
