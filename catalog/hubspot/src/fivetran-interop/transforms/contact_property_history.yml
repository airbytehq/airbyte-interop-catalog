domain: hubspot.fivetran-interop
transforms:
  - name: contact_property_history
    from:
      - contacts_property_history: hubspot.contacts_property_history
    fields:
      _fivetran_synced:
        expression: contacts_property_history._airbyte_extracted_at
        description: '{{ doc("_fivetran_synced") }}'
      contact_id:
        expression: MISSING
        description: The ID of the related contact record.
      name:
        expression: MISSING
        description: '{{ doc("history_name") }}'
      source:
        expression: MISSING
        description: '{{ doc("history_source") }}'
      source_id:
        expression: MISSING
        description: '{{ doc("history_source_id") }}'
      timestamp:
        expression: contacts_property_history.timestamp
        description: '{{ doc("history_timestamp") }}'
      value:
        expression: contacts_property_history.value
        description: '{{ doc("history_value") }}'
annotations:
  approved: false
  missing_fields:
    - contact_id
    - name
    - source
    - source_id
  evaluation:
    source_stream_name: contacts_property_history
    target_table_name: contact_property_history
    table_match_score: 0.6
    completion_score: 0.42857142857142855
    explanation:
      The mapping involves 7 fields, out of which 4 had direct matches.
      However, 3 fields are marked as 'MISSING', significantly reducing the completion
      score.
    field_mapping_evals:
      - name: _fivetran_synced
        expression: contacts_property_history._airbyte_extracted_at
        score: 1.0
        explanation: Standard mapping for '_fivetran_synced' to source stream's '_airbyte_extracted_at'.
      - name: contact_id
        expression: MISSING
        score: 0.0
        explanation: No good match found.
      - name: name
        expression: MISSING
        score: 0.0
        explanation: No good match found.
      - name: source
        expression: MISSING
        score: 0.0
        explanation: No good match found.
      - name: source_id
        expression: MISSING
        score: 0.0
        explanation: No good match found.
      - name: timestamp
        expression: contacts_property_history.timestamp
        score: 0.7
        explanation:
          Reasonable confidence that 'contacts_property_history.timestamp'
          is the correct mapping.
      - name: value
        expression: contacts_property_history.value
        score: 0.7
        explanation:
          Reasonable confidence that 'contacts_property_history.value'
          is the correct mapping.
