domain: github.airbyte-interop
transforms:
  - name: issue_label
    from:
      - issue_labels: github.issue_labels
    fields:
      issue_id:
        expression: MISSING
        description: Foreign key that references the issue table
      label_id:
        expression: MISSING
        description: Unique identifier of the used label
annotations:
  approved: false
  missing_fields:
    - issue_id
    - label_id
  evaluation:
    source_stream_name: issue_labels
    target_table_name: issue_label
    table_match_score: 0.0
    completion_score: 0.0
    explanation: Given the mappings provided, both fields are set to 'MISSING' indicating
      no good match found for any of the fields, leading to a total lack of reliable
      field mappings and thus a complete absence of confidence in this table mapping.
    field_mapping_evals:
      - name: issue_id
        expression: MISSING
        score: 0.0
        explanation: No good match found.
      - name: label_id
        expression: MISSING
        score: 0.0
        explanation: No good match found.
